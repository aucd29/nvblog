<layout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable name="model" type="com.example.nvblog.ui.main.MainViewModel"/>
        <variable name="naviModel" type="com.example.nvblog.ui.navigation.NavigationViewModel"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/main_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

        <androidx.constraintlayout.widget.ConstraintLayout
                android:id="@+id/main_titlebar"
                android:layout_width="match_parent"
                android:layout_height="?actionBarSize"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                android:background="@color/colorPrimary"
        >
            <ImageView
                    android:id="@+id/main_logo"
                    android:layout_width="wrap_content" android:layout_height="wrap_content"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent"
                    android:contentDescription="@string/cd_main_logo"
                    android:src="@drawable/ic_android_white_24dp"
                    android:padding="@dimen/dp10"
                    android:onClick="@{() -> model.command(model.CMD_MOVE_FIRST_TAB)}"
            />

            <androidx.appcompat.widget.AppCompatTextView
                    android:id="@+id/main_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent"
                    android:padding="@dimen/dp10"
                    android:text="@string/main_title"
                    android:textColor="@android:color/white"
                    android:textSize="@dimen/sp15"
                    android:textStyle="bold"
                    android:onClick="@{() -> model.command(model.CMD_GROUP_DIALOG)}"
            />

            <ImageView
                    android:id="@+id/main_search"
                    android:layout_width="wrap_content" android:layout_height="wrap_content"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintEnd_toStartOf="@+id/main_my"
                    app:layout_constraintBottom_toBottomOf="parent"
                    android:contentDescription="@string/cd_main_logo"
                    android:src="@drawable/ic_search_white_24dp"
                    android:layout_marginEnd="@dimen/dp10"
                    android:onClick="@{() -> model.command(model.CMD_SEARCH_FRAGMENT)}"
            />

            <androidx.appcompat.widget.AppCompatTextView
                    android:id="@+id/main_my"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent"
                    android:padding="@dimen/dp10"
                    android:text="@string/main_my"
                    android:textColor="@android:color/white"
                    android:textSize="@dimen/sp20"
                    android:onClick="@{() -> naviModel.command(naviModel.CMD_SHOW_NAVI)}"
            />

        </androidx.constraintlayout.widget.ConstraintLayout>

        <com.example.nvblog.common.widget.VerticalSwipeRefreshLayout
                android:id="@+id/main_swipe_refresh"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/main_titlebar"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                android:fillViewport="true"
        >
            <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/main_content_recycler"
                    android:layout_width="match_parent"
                    android:layout_height="0dp"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                    app:bindAdapter="@{model.adapter}"
                    app:bindItems="@{model.items}"
                    app:bindHorDecoration="@{model.horDecoration}"
                    app:bindVerDecoration="@{model.verDecoration}"
                    android:background="@android:color/white"
            />
        </com.example.nvblog.common.widget.VerticalSwipeRefreshLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
